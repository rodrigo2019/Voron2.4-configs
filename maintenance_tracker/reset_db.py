import sys

import requests

if __name__ == "__main__":

    print("OI")
    # arg = sys.argv[1].lower()
    # if arg == "init_km":
    #     initial_km = float(sys.argv[2])
    # else:
    #     initial_km = 0
    # url = f"http://192.168.100.96:7125/server/database/item?namespace=maintenance_tracker&key=init_value&value={initial_km * 1000 * 1000}`"
    # requests.post(url)
    # url = f"http://192.168.100.96:7125/server/database/item?namespace=maintenance_tracker&key=curr_value&value={initial_km * 1000 * 1000}`"
    # requests.post(url)
    # print(f"Database reset to: {initial_km} km")